export interface OperatorParams{
    limit: number;
    offset: number;
    search: string;
    standard: string;
    country: string;
    state: string;
    blocked: boolean;
    approved: boolean;
}